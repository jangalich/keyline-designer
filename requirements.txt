requests>=2.31.0
flask>=3.0.0
flask-cors>=4.0.0
pystac-client>=0.9.0
planetary-computer>=1.0.0
rasterio>=1.4.0
shapely>=2.1.0
numpy>=2.0.0
pillow>=10.0.0
contourpy>=1.2.0
# PINNED. CSS custom properties, WOFF2 loading and variable-font weights
# were each verified on exactly this version (site-data-report-proposal.md,
# phase 2); a resolver picking a different release could lose any of them
# silently. Bump deliberately, re-running test_site_report.py.
weasyprint==70.0
jinja2>=3.1.0
gunicorn>=22.0.0
